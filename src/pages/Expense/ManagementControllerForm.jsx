import React, { useState, useEffect, useRef } from 'react'

function ManagementControllerForm() {
    // States


    // Hooks

    // SideEffects

    // Handlers
    const handleSubmit=async(e)=>{
        e.preventDefault();
    }
  return (
    <form onChange={handleSubmit}>

    </form>
  )
}

export default ManagementControllerForm